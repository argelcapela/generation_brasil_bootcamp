package ex00;

public class sorteia 
{
	public static void main(String[] args)
	{
		System.out.println((int) ((Math.random() * (6 - 1)) + 1)); // SORTEIA um número entre um range
																   // (int) ((Math.random() * (MAX - MIN)) + MIN); 
	}
}
